import { BodyDirectDirective } from './body-direct.directive';

describe('BodyDirectDirective', () => {
  it('should create an instance', () => {
    const directive = new BodyDirectDirective();
    expect(directive).toBeTruthy();
  });
});
